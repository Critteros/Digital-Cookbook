type Query {
  recipeById(id: ID!): Recipe
}

type Recipe {
  id: ID!
  title: String!
  description: String!
  ingredients: [String!]!
  steps: [String!]!
  image: String
  preparationTimeMinutes: Int
  cookingTimeMinutes: Int
  totalTimeMinutes: Int
  servings: Int
}
